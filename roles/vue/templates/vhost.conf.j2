server {
    listen 80;
    server_name {{item.domain}};

    location / {
        root /home/ubuntu/web-root/{{item.app_name}};
        index index.html index.htm;
        try_files $uri $uri/ /index.html; 
    }
}   

